# X / Twitter Drafts

> Required tags:
>
> - `#0GHackathon`
> - `#BuildOn0G`
> - `@0G_labs`
> - `@0g_CN`
> - `@HackQuest_`

---

## Draft 1 — Recommended English Version

We built **0G OpenClaw Memory Runtime** — a durable memory layer for OpenClaw-style agent workflows on 0G.

- Go orchestrator for workflow ingest
- Rust deterministic checkpoints
- 0G Storage for persistent workflow state
- On-chain verification anchor path

Replayable. Resumable. Verifiable.

Demo: `TODO`

@0G_labs @0g_CN @HackQuest_  
#0GHackathon #BuildOn0G

---

## Draft 2 — Bilingual Version

Introducing **0G OpenClaw Memory Runtime** — a durable memory + verification layer for OpenClaw-style agent workflows on 0G.

用 Go 做编排，用 Rust 做 deterministic checkpoint，把 workflow memory 持久化到 0G Storage，并通过链上 anchor 做可验证证明。

Demo: `TODO`

@0G_labs @0g_CN @HackQuest_  
#0GHackathon #BuildOn0G

---

## Draft 3 — Chinese Version

我们做了 **0G OpenClaw Memory Runtime**：一个面向 OpenClaw 风格 Agent 工作流的 **长期记忆 + 可验证执行** 基础设施。

- Go 编排层
- Rust deterministic checkpoint
- 0G Storage 持久化 workflow state
- 链上 anchor 验证路径

让 Agent workflow 可以 **replay / resume / verify**，不再只是进程内临时状态。

Demo: `TODO`

@0G_labs @0g_CN @HackQuest_  
#0GHackathon #BuildOn0G

---

## Suggested Attachments

Attach at least one of:

1. `/health` + ingest + replay terminal screenshot
2. checkpoint output showing `latest_root` and `latest_cid`
3. contract / explorer screenshot
4. short demo video thumbnail

---

## Before Posting

- Replace `TODO` with the public demo link
- Make sure the repo is public or judge-accessible
- Prefer one clear product screenshot instead of many noisy screenshots
- Pin the post until the hackathon review window ends
